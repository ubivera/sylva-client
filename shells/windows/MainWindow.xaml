<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Ubivera.Sylva.Client.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ubivera.Sylva.Client"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Sylva Hub">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <!-- Mica shows through the whole window, including the custom title bar
         (extended in code-behind). Row 0 is the draggable title bar; row 1 hosts
         the navigation frame + the launch splash. -->
    <Grid RowDefinitions="Auto,*">
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="40" ColumnDefinitions="Auto,Auto">
            <Image Grid.Column="0" Source="ms-appx:///Assets/Square44x44Logo.scale-200.png"
                   Width="18" Height="18" Margin="14,0,0,0" VerticalAlignment="Center" />
            <TextBlock Grid.Column="1" Text="Sylva Hub" Margin="10,0,0,0" VerticalAlignment="Center"
                       Style="{StaticResource CaptionTextBlockStyle}" />
        </Grid>

        <Grid Grid.Row="1">
            <Frame x:Name="RootFrame" />
            <ProgressRing x:Name="Splash" IsActive="True" Width="48" Height="48"
                          HorizontalAlignment="Center" VerticalAlignment="Center" />
        </Grid>
    </Grid>
</Window>
