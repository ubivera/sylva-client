<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Ubivera.Sylva.Client.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Ubivera.Sylva.Client"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="48">
        <StackPanel Spacing="16" HorizontalAlignment="Center" VerticalAlignment="Center" MaxWidth="480">
            <!-- Initials avatar for now; a real photo replaces it in CP3. -->
            <PersonPicture x:Name="Avatar" Width="120" Height="120" HorizontalAlignment="Center" />
            <TextBlock x:Name="WelcomeText" Style="{StaticResource TitleTextBlockStyle}"
                       HorizontalAlignment="Center" TextAlignment="Center" />
            <TextBlock x:Name="EmailText" Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                       HorizontalAlignment="Center" />
            <ProgressRing x:Name="Busy" IsActive="True" HorizontalAlignment="Center" />
            <InfoBar x:Name="ErrorBar" Severity="Error" IsOpen="False" IsClosable="True" />
        </StackPanel>
    </Grid>
</Page>
